<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Unsupervised Learning</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: Arial, sans-serif;
    }

    body {
      background: #0f172a;
      color: #e2e8f0;
      scroll-behavior: smooth;
    }

    header {
      background: #020617;
      padding: 20px;
      text-align: center;
      position: sticky;
      top: 0;
      z-index: 1000;
      animation: fadeDown 1s ease;
    }

    header h1 {
      font-size: 2.5rem;
      color: #38bdf8;
    }

    nav {
      display: flex;
      justify-content: center;
      gap: 20px;
      margin-top: 10px;
    }

    nav a {
      color: #e2e8f0;
      text-decoration: none;
      font-weight: bold;
      transition: 0.3s;
    }

    nav a:hover {
      color: #38bdf8;
    }

    .hero {
      text-align: center;
      padding: 80px 20px;
      animation: fadeIn 1.5s ease;
    }

    .hero h2 {
      font-size: 2.2rem;
      margin-bottom: 15px;
    }

    .hero p {
      max-width: 600px;
      margin: auto;
      margin-bottom: 25px;
      color: #94a3b8;
    }

    .btn {
      background: #38bdf8;
      color: #020617;
      padding: 12px 25px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: 0.3s;
    }

    .btn:hover {
      transform: scale(1.05);
      box-shadow: 0 0 15px #38bdf8;
    }

    .section {
      padding: 60px 20px;
      text-align: center;
    }

    .section h2 {
      margin-bottom: 20px;
      font-size: 2rem;
    }

    .cards {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 25px;
      margin-top: 20px;
    }

    .card {
      background: #020617;
      padding: 25px;
      width: 260px;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.5);
      transition: 0.4s;
      opacity: 0;
      transform: translateY(30px);
      animation: fadeUp 1s ease forwards;
    }

    .card:hover {
      transform: translateY(-10px) scale(1.03);
      box-shadow: 0 10px 30px rgba(56,189,248,0.3);
    }

    .card h3 {
      color: #38bdf8;
      margin-bottom: 10px;
    }

    footer {
      text-align: center;
      padding: 20px;
      background: #020617;
      margin-top: 40px;
      color: #94a3b8;
      animation: fadeIn 2s ease;
    }

    /* Animations */
    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    @keyframes fadeUp {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes fadeDown {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Delay for cards */
    .card:nth-child(1) { animation-delay: 0.2s; }
    .card:nth-child(2) { animation-delay: 0.4s; }
    .card:nth-child(3) { animation-delay: 0.6s; }

  </style>
</head>
<body>

<header>
  <h1>Unsupervised Learning</h1>
  <nav>
    <a href="#">Home</a>
    <a href="#concepts">Concepts</a>
    <a href="#applications">Applications</a>
  </nav>
</header>

<section class="hero">
  <h2>Discover Hidden Patterns in Data</h2>
  <p>Unsupervised learning finds patterns in data without labeled outputs. It helps uncover structure, clusters, and insights automatically.</p>
  <button class="btn">Explore Now</button>
</section>

<section id="concepts" class="section">
  <h2>Key Concepts</h2>
  <div class="cards">
    <div class="card">
      <h3>Clustering</h3>
      <p>Grouping similar data points based on features.</p>
    </div>
    <div class="card">
      <h3>K-Means</h3>
      <p>A widely used clustering algorithm.</p>
    </div>
    <div class="card">
      <h3>Dimensionality Reduction</h3>
      <p>Reducing features while preserving key patterns.</p>
    </div>
  </div>
</section>

<section id="applications" class="section">
  <h2>Real-World Applications</h2>
  <div class="cards">
    <div class="card">
      <h3>Customer Segmentation</h3>
      <p>Analyze users and group them by behavior.</p>
    </div>
    <div class="card">
      <h3>Anomaly Detection</h3>
      <p>Detect fraud or unusual patterns in data.</p>
    </div>
    <div class="card">
      <h3>Recommendation Systems</h3>
      <p>Suggest content based on patterns.</p>
    </div>
  </div>
</section>

<footer>
  <p>© 2026 Unsupervised Learning | Dark Theme UI</p>
</footer>

</body>
</html>
